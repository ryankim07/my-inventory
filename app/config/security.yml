# To get started with security, check out the documentation:
# http://symfony.com/doc/current/security.html
security:

    encoders:
        AppBundle\Entity\Users\UsersEntity:
            algorithm: bcrypt

    providers:
        database_users:
            entity:
                class: 'AppBundle\Entity\Users\UsersEntity'
                property: username

    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false

        login:
            pattern: ^/auth
            anonymous: true
            stateless: true
            guard:
                authenticators:
                    - 'Username_Password_Authenticator'

        api:
            pattern: ^/api
            anonymous: false
            stateless: true
            guard:
                authenticators:
                    - 'Jwt_Token_Authenticator'

        registration:
            pattern: ^/registration
            anonymous: true
            stateless: true
            security: false

        main:
            pattern: ^/
            anonymous: ~
            provider: database_users

    access_control:
        - { path: ^/login, roles: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/api, roles: [ROLE_ADMIN, ROLE_USER] }
        - { path:  ^/registration, roles: IS_AUTHENTICATED_ANONYMOUSLY }